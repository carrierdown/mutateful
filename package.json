{
  "name": "live-clip-processor",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "test": "tsc && tsc -p tsconfig-test.json && cat clip-processor.js compiled/index.spec.js > compiled/tests.js && tape compiled/tests.js | tap-spec",
    "mu4l": "tsc -p tsconfig-mu4l.json"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
  },
  "devDependencies": {
    "tap-spec": "^4.1.1",
    "tape": "~2.4.0"
  }
}
