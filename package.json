{
  "name": "live-clip-processor",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "tsc -p tsconfig-test.json && cat clip-processor.js compiled/index.spec.js > compiled/tests.js && tape compiled/tests.js | tap-spec"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
  },
  "devDependencies": {
    "tap-spec": "^4.1.1",
    "tape": "~2.4.0"
  }
}
